Added the ability to use multiple templates

HOW TO:
Create new tpl files in /layout/templates/widgets with names "show_myTemplateName.tpl"
Then add them to the dropdown in the (backend) /model/engine.php getTemplatesForDropdown() function